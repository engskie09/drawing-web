export interface TenderDrawing {
  no: string,
  title: string,
  targetDate: string,
  titleDate: string,
  drawingFile: string,
  category: string,
  projectId: string,
  employeeId: string,
  id: string,
  currentDrawingNo?: string,
  categoryName?: string;
  employeeName?: string;
  projectName?: string;
}